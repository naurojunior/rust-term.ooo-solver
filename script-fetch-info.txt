let elements = [];

Array.from($("#board0").shadowRoot
						.children['hold']
						.children[0]
						.shadowRoot
						.childNodes)
			.slice(1)
			.forEach((element => elements.push({"letter": element.textContent, 
											   "position": parseInt(element.getAttribute('termo-pos')), 
											   "rule_type": element.classList[1]})));
											   
JSON.stringify(elements);